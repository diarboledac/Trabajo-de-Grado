version: "3.8"

services:
  tb-load-lab:
    build: .
    image: tb-load-lab:latest
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    command: >-
      python3 scripts/mqtt/run_stress_suite.py
      --device-count 20
      --duration 120
      --deactivate-after
    tty: true
