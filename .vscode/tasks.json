{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Run All (bootstrap)",
      "type": "shell",
      "command": "${command:python.interpreterPath}",
      "args": ["bootstrap.py", "--all"],
      "problemMatcher": []
    }
  ]
}
